using UnityEngine;

namespace NewProject.CodeBase.UI
{
    public class HandCardDragHover : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
